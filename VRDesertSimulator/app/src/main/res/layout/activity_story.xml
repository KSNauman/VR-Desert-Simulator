<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_story"
    android:padding="32dp">

    <TextView
        android:id="@+id/storyText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="You survived a plane crash.\nStranded in a vast desert,\nyou must gather resources to survive.\n\nFind water, food, wood, and matches.\nBuild a fire. Signal for rescue."
        android:textSize="20sp"
        android:textColor="#FFDDAA66"
        android:gravity="center"
        android:lineSpacingExtra="8dp"
        android:fontFamily="serif"
        android:shadowColor="#88000000"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="4" />

    <Button
        android:id="@+id/btnStartGame"
        style="@style/DesertButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"
        android:text="START GAME" />
</RelativeLayout>
